import React from 'react'
import ReactDOM from 'react-dom/client'
import MortgageLandingPage from '../mortgage_landing_page'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MortgageLandingPage />
  </React.StrictMode>,
)